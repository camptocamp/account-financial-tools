<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="sale_order_line_pivot" model="ir.ui.view">
            <field name="name">sale.order.line.pivot</field>
            <field name="model">sale.order.line</field>
            <field name="arch" type="xml">
                <pivot string="Timesheet">
                    <field name="order_id" type="row"/>
                    <field name="product_id" type="col"/>
                    <field name="amount_invoiced_holding_currency" type="measure"/>
                    <field name="amount_delivered_holding_currency" type="measure"/>
                </pivot>
            </field>
        </record>

</odoo>